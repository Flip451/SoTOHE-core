//! T008: `type_graph_cluster` is deleted.
//!
//! `classify_types(graph: &TypeGraph, ...)` is removed because `TypeGraph`
//! no longer exists.  The `sotp track type-graph` command is deleted in T008.
