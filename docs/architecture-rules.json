{
  "version": 2,
  "module_limits": {
    "max_lines": 700,
    "warn_lines": 400,
    "exclude": ["vendor/", ".cache/"]
  },
  "canonical_modules": [
    {
      "concern": "shell-parsing",
      "owner": "libs/domain/src/guard/parser.rs",
      "forbidden_patterns": [
        "fn\\s+normalize_separators",
        "fn\\s+shell_tokenize",
        "fn\\s+is_redirect_operator",
        "fn\\s+split_shell_tokens",
        "matches!\\(token,\\s*\">\"",
        "\\.split_whitespace\\(\\).*is_rm",
        "prev_was_fd_digit"
      ],
      "allowed_in": ["libs/domain/src/guard/"],
      "convention": "project-docs/conventions/shell-parsing.md",
      "rationale": "All shell command parsing must use conch-parser via domain::guard::parser"
    }
  ],
  "extra_dirs": [
    {
      "path": "project-docs/conventions",
      "label": "project-specific engineering rules"
    },
    {
      "path": "track",
      "label": "track workflow state and planning artifacts"
    },
    {
      "path": "track/items/<id>",
      "label": "active track directory"
    },
    {
      "path": "track/archive/<id>",
      "label": "archived track directory"
    }
  ],
  "layers": [
    {
      "crate": "domain",
      "path": "libs/domain",
      "may_depend_on": [],
      "deny_reason": "Domain dependencies are controlled by layer boundaries."
    },
    {
      "crate": "usecase",
      "path": "libs/usecase",
      "may_depend_on": [
        "domain"
      ],
      "deny_reason": "Usecase crate must only be consumed by the cli layer."
    },
    {
      "crate": "infrastructure",
      "path": "libs/infrastructure",
      "may_depend_on": [
        "domain",
        "usecase"
      ],
      "deny_reason": "Infrastructure implements usecase ports and domain ports; wired at composition root."
    },
    {
      "crate": "cli",
      "path": "apps/cli",
      "may_depend_on": [
        "domain",
        "infrastructure",
        "usecase"
      ],
      "deny_reason": ""
    }
  ]
}
