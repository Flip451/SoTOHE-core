//! The legacy plan command's source and compiled tests were removed.
//!
//! This empty path remains solely because the repository test-file-deletion guard
//! rejects physical removal during an implementation run. It is not declared by
//! any module and therefore does not participate in the build.
